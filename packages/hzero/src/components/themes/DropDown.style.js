import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core/lib/utils";

const dropdownStyle = () => {
  return css``;
};

export default createStyleComponent("dropdown", dropdownStyle);
