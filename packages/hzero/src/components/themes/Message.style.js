import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core/lib/utils";

const styles = () => {
  return css``;
};

export default createStyleComponent("message", styles);
