import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core/lib/utils";

const Uploadstyles = () => {
  return css``;
};

export default createStyleComponent("upload", Uploadstyles);
