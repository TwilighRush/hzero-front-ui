import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core/lib/utils";

const treeSelectStyles = () => {
  return css``;
};

export default createStyleComponent("treeSelect", treeSelectStyles);
