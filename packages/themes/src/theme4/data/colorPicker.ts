import { ComponentData } from '@hzero-front-ui/core';
import colorPickerStyle from '../templates/ColorPicker.style';

const colorPickerData: ComponentData = {
  name: 'colorPicker',
  data: [{}],
  onlyTemplate: { pro: colorPickerStyle },
};

export default colorPickerData;
