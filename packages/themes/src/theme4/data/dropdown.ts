import { ComponentData } from '@hzero-front-ui/core';
import DropdownStyle from '../templates/Dropdown.style';

const badgeData: ComponentData = {
  name: 'dropdown',
  data: [{}],
  onlyTemplate: { pro: DropdownStyle },
};

export default badgeData;
