import { ComponentData } from '@hzero-front-ui/core';
import RateStyle from '../templates/Rate.style';

const rate: ComponentData = {
  name: 'rate',
  data: [
    {
      Color: '#FFB040',
      fontSize: 24,
    },
  ],
  onlyTemplate: RateStyle,
};
export default rate;
