import { css } from 'styled-components';
import { ComponentData } from '@hzero-front-ui/core';
import IconStyle from '../templates/Icon.style';
const iconData: ComponentData = {
  name: 'icon',
  data: [],
  onlyTemplate: IconStyle,
};

export default iconData;
