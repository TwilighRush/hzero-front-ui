import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core";

function getAttachmentStyle() {
  return css``;
}

export default createStyleComponent("attachment", getAttachmentStyle);
