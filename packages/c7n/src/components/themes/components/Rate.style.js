import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core";

function getRateStyle() {
  return css``;
}

export default createStyleComponent("comp/rate", getRateStyle);
