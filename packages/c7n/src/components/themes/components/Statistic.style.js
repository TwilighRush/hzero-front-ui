import { css } from "styled-components";
import { createStyleComponent } from "@hzero-front-ui/core";

function getStatisticStyle() {
  return css``;
}

export default createStyleComponent("comp/statistic", getStatisticStyle);
